specVersion: "v1"
serviceId: "yuva"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "yuva-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
